{
  // Optional: Path to WinSCP executable or folder
  // Can be either:
  //   - Full path to winscp.com: "C:\\Program Files\\WinSCP\\winscp.com"
  //   - Folder path: "D:\\Progz\\dev\\WinSCP" (will append winscp.com automatically)
  // If not provided, WinSCP will be searched in PATH and common installation locations
  winscpPath: "D:\\path\\to\\WinSCP",

  projects: [
    {
      name: "My Project 1",
      localPath: "D:\\my\\local\\path",
      remotePath: "/www/my_project",
    },
    {
      name: "My Project 2",
      localPath: "D:\\another\\local\\path",
      remotePath: "/www/another_project",
    },
  ],

  ftp: {
    host: "11.22.33.44",
    port: 22,
    username: "your-username",
    password: "your-password",
    passiveMode: false, // Only relevant for FTP. Set to true if FTP is behind a firewall.
    secure: true,  // Set to true for SFTP, false for FTP
  },

  // File extensions ignored during ANALYSIS only (they will still be synced). Use this to speed up analysis
  excludedExtensionsFromAnalysis: [
    "txt",     // Documentation/readme files that cause timestamp mismatches
    "tmp",
    "bak",
    "log",
  ],

  // File extensions ignored from sync (and therefore also from analysis, because there's no need to analyze them)
  excludedExtensionsFromSync: [],

  // Specific filenames ignored during ANALYSIS only (they will still be synced) (case insensitive). Use this to speed up analysis
  excludedFilesFromAnalysis: [
    "readme.txt",
    ".ds_store",
    "thumbs.db",
  ],


  // Specific filenames ignored from sync (and therefore also from analysis, because there's no need to analyze them) (case insensitive)
  excludedFilesFromSync: [".npmrc"],

  // Folders ignored during ANALYSIS only (they will still be synced). Use this to speed up analysis
  excludedFoldersFromAnalysis: [
    "assets",
    "static",
    "uploads",
  ],

  // Folders ignored from sync (and therefore also from analysis, because there's no need to analyze them)
  excludedFoldersFromSync: [
    "node_modules",
    "output",
    "build",
    "dist",
  ],
}
